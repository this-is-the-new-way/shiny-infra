{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Sample application for ECS deployment",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'No build process needed for this sample'",
    "test": "jest",
    "lint": "eslint src/",
    "docker:build": "docker build -t my-app:latest .",
    "docker:run": "docker run -p 3000:3000 my-app:latest"
  },
  "keywords": [
    "nodejs",
    "express",
    "docker",
    "ecs",
    "aws"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "winston": "^3.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "eslint": "^8.45.0",
    "jest": "^29.6.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
